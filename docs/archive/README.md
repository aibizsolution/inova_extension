# Archived Reference Docs

이 폴더의 문서는 기본 작업 시작 시 읽지 않는다.

- 과거 버전 판단, remote capability 설계 배경, stale handoff 확인이 필요할 때만 참조한다.
- 현재 운영 기준은 `README.md`, `AGENTS.md`, `docs/feature-routing.md`, `docs/runtime-architecture.md`, `docs/release-workflow.md`, 그리고 feature별 `AGENTS.md`를 우선한다.
- 이 폴더의 문서는 최신 코드와 맞도록 계속 유지보수하지 않는다.
- archive 문서와 현재 코드/운영 문서가 충돌하면 현재 코드와 운영 문서를 우선한다.

